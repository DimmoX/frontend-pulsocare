server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        # Intenta cargar la ruta exacta, si no existe, redirige al index.html de Angular
        try_files $uri $uri/ /index.html;
    }
}
